{
    "params": [
        {
            "key": "fastqs_dir",
            "is_storage": true,
            "is_required": true,
            "help": "The inputs folder which contains the fastq files, may have another files but takes all"
        },
        {
            "key": "output_folder",
            "is_storage": true,
            "is_required": true,
            "help": "The inputs folder which contains the fastq files, may have another files but takes all"
        }
    ]
}